// asyncify-fast is the asyncify variant with the Asyncify instrumentation
// narrowed to a curated set of functions (see mpconfigvariant.mk) for a much
// smaller and faster build. The MicroPython configuration is identical.
#include "../asyncify/mpconfigvariant.h"
