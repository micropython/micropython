# Large allocation tests for t-string coverage
# These tests require substantial memory and are unix-only

# Template constructor overflow
Template() overflow: OverflowError (correct)

# Template varargs constructor n_strings overflow
Template varargs n_strings overflow: OverflowError (correct)

# __template__ type validation (list instead of tuple)
__template__ list validation: TypeError (correct)

# __template__ overflow
__template__ overflow: OverflowError (correct)

# Runtime overflow > 4095 interpolations
4096 interpolations (runtime): OverflowError (correct)

# Parse-time overflow > 4095 interpolations
4096 interpolations (parse-time): OverflowError (correct)

# __template__ with maximum valid sizes
Max strings: OK, 100 strings
Max interps error: OverflowError

# Too many segments
Too many segments: OverflowError (correct)

# Approaching max interpolations
4090 interpolations error: OverflowError

# Empty expression (whitespace only)
Empty expr (space): SyntaxError (correct)
Empty expr (tab): SyntaxError (correct)
Empty expr (newline): SyntaxError (correct)

# Lexer escape sequence: invalid then valid
Invalid escape sequence: SyntaxError (correct)

=== Large coverage tests completed! ===
